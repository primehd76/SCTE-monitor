services:
  backend:
    build: ./backend
    container_name: sm_backend
    network_mode: "host"
    environment:
      - PYTHONUNBUFFERED=1
    restart: always
    volumes:
      - ./frontend:/app/frontend
